# dockerps

> A better `docker ps`

## Usage
```bash
# show running containers
$ npx dockerps

# show all containers
$ npx dockerps --all

# show the latest created container
$ npx dockerps --latest

# show 2 last created containers
$ npx dockerps --last 2
```
